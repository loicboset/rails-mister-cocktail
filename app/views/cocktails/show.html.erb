<div class="container">
  <h1><%= @cocktail.name %></h1>
  <%= cl_image_tag @cocktail.photo.key, width: 300, height: 400 if @cocktail.photo.attached? %>
  <%= link_to 'Delete', cocktail_path(@cocktail), method: :delete%>
  <%= link_to 'Edit', edit_cocktail_path(@cocktail) %>
  <%= link_to 'Back', cocktails_path %>
</div>
